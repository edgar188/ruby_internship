.flex-grow-0.py-3.px-4.border-top
  .input-group
  = form_for [@conversation, @message], remote: true  do |f|
    .form-group.d-flex
      = f.hidden_field :conversation_id, value: @conversation.id
      = f.hidden_field :conversation_user_id, value: current_user.correct_conversation_user(@conversation)
      = f.text_field :text, placeholder: 'Your message', autofocus: true, id: 'new_message_input', class: 'form-control'
      = f.submit 'Send', id: 'send_message_btn', class: 'btn btn-primary d-none'